#!/usr/bin/env bash
# This script is displaying a Bash script that displays the content of the file /etc/passwd.

while IFS=: read -r f1 f2 f3 f4 f5 f5 f7
do
	echo "username: $f1"
	echo "user id: $f3"
	echo "home directory path for the user: $f6"
done < /etc/passwd
